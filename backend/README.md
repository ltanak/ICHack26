# Commands

## Create virtual env (with UV if you have aura)

```
cd backend

uv venv

source .venv/bin/activate

uv sync
```

## Run project
Run with:
```
flask run
```

This targets `app.py` by default.